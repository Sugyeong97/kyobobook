import { createAction } from "@reduxjs/toolkit";

export const increaseProduct = createAction("basketCounter/increaseProduct");
export const decreaseProduct = createAction("basketCounter/decreaseProduct");
